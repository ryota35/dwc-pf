<% comments.each do |comment| %>
  <div class="border-bottom d-flex align-items-end mt-3 h4">
    <p class="test flex-grow-1"><%= comment.comment %></p>
    <span class="text-nowrap"><%= comment.user.name %></span>
    <% if comment.user == current_user %>
      <span class="text-nowrap ml-2"><%= link_to "削除", article_comment_path(article_id: article, id: comment.id), method: :delete, remote: true, class:"text-danger" %></span>
    <% end %>
  </div>
<% end %>