<div class="container">

  <%= form_with model: @article, local: true do |f| %>

    <div class="form-group">
    <%= f.label :image %>
    <%= f.attachment_field :image %>
    </div>

    <div class="form-group">
    <%= f.label :title %><br>
    <%= f.text_field :title, size:50 %>
    </div>

    <div class="form-group">
    <%= f.label :body %><br>
    <%= f.text_area :body, row:5, size:50 %>
    </div>

    <div class="form-group">
    <%= f.label :level %><br>
    <%= f.select :level, Article.levels.keys, {prompt: "選択して下さい"} %>
    </div>

    <div class="form-group">
      <%= f.collection_check_boxes(:tag_ids, Tag.all, :id, :name) do |tag| %>
            <span class=""><%= tag.label { tag.check_box + tag.text } %></span>
      <% end %>
    </div>

    <%= f.submit "投稿する", class:"btn btn-success" %>

  <% end %>

  <h1>ご投稿の際の注意事項</h1>

  <ul>
    <li>公序良俗に反しない内容であること</li>
    <li>個人を誹謗・中傷する内容でないこと</li>
    <li>個人情報は投稿しないこと</li>
  </ul>

</div>