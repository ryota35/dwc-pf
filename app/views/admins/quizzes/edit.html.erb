
<div class="container pt-5">
  <div class="row d-flex flex-column align-items-center pt-5">

    <h1 class="pb-4 mb-4">クイズ編集</h1>
    <div class="col-md-8 bg-white p-4">
      <%= form_with model: @quiz, url: admins_quiz_path(@quiz), local:true do |f| %>
        <%= render "quiz-form", quiz: @quiz %>
      <% end %>
    </div>

  </div>
</div>